import { Meta, Story } from '@storybook/react/types-6-0';
import { Cloud } from './index';

export default {
  title: 'Icon',
  component: Cloud
} as Meta;

export const Initial: Story = (args) => <Cloud />;
